#pragma once

namespace segeom {
  namespace primitives {
    template <class T>
    class Polygon {
    private:
      /* data */
    public:
      Polygon(/* args */);
      ~Polygon();
    };
  } // namespace primitives
} // namespace segeom